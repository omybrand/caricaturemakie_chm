<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title><%= title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script>
        function update () {
    	   if (document.getElementById('user_pwd').value == '') {
    	       alert('변경할 비밀번호를 입력해 주세요.');
                document.getElementById('user_pwd').focus();
    	       return;
    	   }
    	   if (document.getElementById('user_pwd2').value == '') {
    	       alert('변경할 비밀번호 확인을 입력해 주세요.');
    	       document.getElementById('user_pwd2').focus();
    	       return;
    	   }
            if (document.getElementById('user_pwd').value != document.getElementById('user_pwd2').value) {
                alert('비밀번호가 서로 다릅니다. 확인해 주세요.');
                return;
            }
            document.updateform.submit();
        }
    </script>
</head>
<body>
        <form class="form-container" id="updateform" name="updateform" action="/chm/user/changepwd" method="post">
        <div class="form-title"><h2><%= title %></h2></div>
        <div class="form-title">변경할 비밀번호</div>
        <input class="form-field" type="password" id="user_pwd" name="user_pwd" maxlength="30"><br>
        <div class="form-title">변경할 비밀번호 확인</div>
        <input class="form-field" type="password" id="user_pwd2" name="user_pwd2" maxlength="30"><br>
        <div class="submit-container">
            <input class="submit-button" type="button" value="변경하기" onclick="update()">
        </div>
    </form>
  </body>
</html>